PORT=3020
HOST=127.0.0.1
CLAWMASTERS_MODE=auto
OPENCLAW_HIDE_AGENT_IDS=skill-installer,lala
# Defaults already point to ~/.openclaw. Uncomment only for custom paths.
# OPENCLAW_STATE_DIR=/home/your-user/.openclaw
# OPENCLAW_CONFIG_PATH=/home/your-user/.openclaw/openclaw.json
# Optional: use this only if you want the dashboard to enforce an exact fleet.
# CLAWMASTERS_EXPECTED_AGENTS=Main,Research,Content,Ops
